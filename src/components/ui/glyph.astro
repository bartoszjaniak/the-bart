---
import { DEFAULT_GLYPH } from "../../lib/glyphs";

interface Props {
	value?: string;
	variant?: string;
}

const { value, variant } = Astro.props;
---

<span class="glyph" data-variant={variant} aria-hidden="true">{value || DEFAULT_GLYPH}</span>
